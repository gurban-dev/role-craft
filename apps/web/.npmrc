# pnpm 10+ blocks dependency build scripts unless allowlisted.
# package.json → pnpm.onlyBuiltDependencies lists esbuild / unrs-resolver.
# This flag keeps Docker/CI installs from failing on ignored-build policy.
dangerouslyAllowAllBuilds=true
